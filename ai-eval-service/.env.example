# FastAPI server configuration
HOST=0.0.0.0
PORT=8000
ENV=development

# Set to any non-empty value when running on Render (Render auto-sets this)
RENDER=

# LangFair / Google Gemini configuration
GEMINI_API_KEY=your_gemini_api_key

# Evaluation tuning knobs (safe defaults shown)
LIGHTWEIGHT_EVAL_MODE=true
TOXICITY_BATCH_SIZE=32
# Comma-separated toxicity classifier overrides (keep blank to use defaults)
TOXICITY_CLASSIFIERS=
