# Core LangFair and evaluation dependencies
# Note: torch is installed separately to use CPU-only version (saves ~1.5GB)
# Install torch first: pip install torch --index-url https://download.pytorch.org/whl/cpu
langfair>=0.1.0

# Web framework for API server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# LangChain (required by LangFair, but we don't use it directly for LLM)
# Note: Counterfactual evaluation is currently disabled due to version conflicts
# Toxicity and stereotype metrics work without LLM

# Environment variable management
python-dotenv>=1.0.0

# Data validation (included with FastAPI but explicit)
pydantic>=2.0.0

